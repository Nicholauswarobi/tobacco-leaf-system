# Extra dependencies for building the Not_Tobacco negative class.
#
# Kept separate from requirements.txt on purpose: FiftyOne bundles its own
# MongoDB and pulls a large dependency tree, and it is only needed for the
# one-off dataset download — not for training, and not for serving.
#
#   pip install -r requirements-verification.txt
#   python scripts/download_not_tobacco.py --per-class 150
#
# Once ml/data/raw/not_tobacco_source/ is populated you can uninstall it; the
# rest of the verification pipeline has no FiftyOne dependency.

-r requirements.txt

fiftyone>=1.0.0
# FiftyOne's Open Images integration needs boto3 to reach the public S3 bucket.
boto3>=1.34.0

# Alternative CLI downloader, if you prefer it to FiftyOne's zoo:
#   openimages>=0.2.0
